[feat]: Add news bot with reusable online/memory services

Description:

  - Created news bot (Delilah) with AI prefix-only + deterministic rendering
  - Added catcord-online service for RSS/Atom fetching with caching
  - Added catcord-memory service for RAG/event storage
  - Online service enforces room allowlist via ONLINE_ALLOWLIST_ROOMS env var
  - News bot passes timeout_s and user_agent to online service
  - Services are internal-only (docker network isolation)
  - Removed deprecated fetch.py from news bot
  - Fixed all tests (42/42 passing) with proper isolation
  - Updated docker-compose with healthchecks for services
  - Verified all config templates match their configs

Additional info:

  - News bot follows same pattern as cleaner bot (FrameworkConfig, dataclasses)
  - Online service: ETag/Last-Modified caching, HTML stripping, content-hash dedupe, 403 for non-allowlisted rooms
  - Memory service: Person-aware event storage, query by filters
  - Each bot has own config.yaml (no shared .env except optional ONLINE_ALLOWLIST_ROOMS)
  - LLM generates prefix only, never facts (anti-hallucination)
  - All news items rendered deterministically from RSS payload
  - Added fastapi/uvicorn to requirements.txt for service tests
